const SectionTag = ({ children }) => (
  <span className="inline-block text-xs font-semibold uppercase tracking-[0.2em] mb-4 px-4 py-1.5 rounded-full bg-[#C0392B] text-white">
    {children}
  </span>
);

export default SectionTag;
